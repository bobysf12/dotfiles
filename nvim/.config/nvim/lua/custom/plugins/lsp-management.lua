-- Global LSP keymaps moved to lua/custom/lsp_keymaps.lua and required from
-- init.lua. Keeping this spec empty avoids declaring nvim-lspconfig twice
-- (which caused lazy.nvim to merge specs and drop the real LSP config).
return {}
